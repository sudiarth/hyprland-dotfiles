{
    "layer": "top",
    "position": "bottom",
    "height": 28,
    "spacing": 0,
    "margin-left": 11,
    "margin-right": 11,
    "margin-bottom": 6,
    "modules-left": ["hyprland/workspaces"],
    "modules-right": ["cpu", "temperature", "memory", "custom/warp", "network", "pulseaudio", "battery", "clock", "tray"],

    "hyprland/workspaces": {
        "format": "{id}",
        "on-click": "activate",
        "sort-by-number": true
    },

    "cpu": {
        "interval": 5,
        "format": " {usage}%",
        "tooltip": false
    },

    "temperature": {
        "thermal-zone": 0,
        "format": " {temperatureC}°C",
        "tooltip": false
    },

    "memory": {
        "interval": 5,
        "format": " {percentage}%",
        "tooltip": false
    },

    "network": {
        "interface": "wlp2s0",
        "format-wifi": " {essid}",
        "format-disconnected": " Disconnected",
        "tooltip": false
    },

    "pulseaudio": {
        "format": " {volume}%",
        "format-muted": " Muted",
        "on-click": "pavucontrol",
        "tooltip": false
    },

    "battery": {
        "bat": "BAT0",
        "interval": 10,
        "format": "{icon} {capacity}%",
        "format-icons": ["", "", "", "", ""],
        "format-charging": " {capacity}%",
        "tooltip": false
    },

    "clock": {
        "format": " {:%b %d at %H:%M}",
        "tooltip": false
    },

    "tray": {
        "spacing": 8
    },

    "custom/warp": {
        "exec": "status=$(warp-cli --accept-tos status 2>/dev/null | head -1 | awk '{print $NF}'); if [ \"$status\" = 'Connected' ]; then echo ' WARP'; else echo ' OFF'; fi",
        "interval": 10,
        "on-click": "warp-cli --accept-tos connect",
        "on-click-right": "warp-cli --accept-tos disconnect",
        "tooltip": false
    }
}
